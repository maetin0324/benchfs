#!/bin/bash
# BenchFS Job Parameters Configuration - Small transfer size for testing
# Using the same parameters as perf-debug job which worked correctly

# IOR Transfer sizes - using 4m which worked in perf-debug
transfer_size_list=(
  4m   # 4 MiB - worked in perf-debug
)

# IOR Block sizes - using 64m which worked in perf-debug
block_size_list=(
  64m  # 64 MiB - worked in perf-debug
)

# Processes per node - single process for testing
ppn_list=(
  1
)

# IOR flags
ior_flags_list=(
  "-w -r -F"  # Separate files per process
)

# BenchFS chunk sizes (in bytes)
benchfs_chunk_size_list=(
  4194304   # 4 MiB (baseline)
)
