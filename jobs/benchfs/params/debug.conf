#!/bin/bash
# BenchFS Job Parameters - Debug/Troubleshooting
# Purpose: Minimal parameters for debugging issues
# Expected duration: ~30 seconds

transfer_size_list=(
  4m    # 4 MiB - known working size
)

block_size_list=(
  64m   # 64 MiB - known working size
)

ppn_list=(
  1     # Single process for easier debugging
)

ior_flags_list=(
  "-w -r -F"  # File per process (simpler)
)

benchfs_chunk_size_list=(
  4194304   # 4 MiB - baseline
)
