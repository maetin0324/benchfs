#!/bin/bash
# Unified Job Parameters - Large Scale Test
# Purpose: Test with many nodes (16+) and high parallelism
# Compatible with: BenchFS, BeeOND, CHFS
# Expected duration: Variable based on node count
# Recommended: Use with gen_M or gen_L queue

# ============================================================
# Common IOR Parameters
# ============================================================

transfer_size_list=(
  1m    # 1 MiB - common size
  # 4m  # 4 MiB - verified stable
)

block_size_list=(
  # 512m # 512 MiB - large blocks for scale
  1g    # 1 GiB
  # 4g  # 4 GiB
)

ppn_list=(
  4
  8
  16
  32
)

ior_flags_list=(
  "-w -r -F"  # File per process (scales better)
  "-w -r"       # Single shared file
)

# ============================================================
# BenchFS-specific Parameters
# ============================================================

server_ppn_list=(
  1
  # 2
  # 4
  # 8
  # 16
)

benchfs_chunk_size_list=(
  32m   # 32 MiB - optimal for large scale
)

# ============================================================
# CHFS-specific Parameters
# ============================================================

CHFS_CHUNK_SIZE=33554432   # 32 MiB
CHFS_PROTOCOL="verbs"
CHFS_DB_SIZE="10GB"
