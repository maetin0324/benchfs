#!/bin/bash
# Direct IOR comparison: ppn=20, b=16g, shared file
# Match io500 ppn=20 setup for direct apples-to-apples comparison

transfer_size_list=(4m)
block_size_list=(16g)
ppn_list=(20)
ior_flags_list=("-w -r")

server_ppn_list=(1)
benchfs_chunk_size_list=(4m)

CHFS_CHUNK_SIZE=4194304
CHFS_PROTOCOL="verbs"
CHFS_DB_SIZE="10GB"
