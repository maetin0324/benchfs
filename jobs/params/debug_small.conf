#!/bin/bash
# Unified Job Parameters - Debug Small Transfer
# Purpose: Debug with small transfer size and high concurrency
# Compatible with: BenchFS, BeeOND, CHFS
# Expected duration: Variable based on node count

# ============================================================
# Common IOR Parameters
# ============================================================

transfer_size_list=(
  4k    # 4 KiB - small transfer
)w
block_size_list=(
  128m
)

ppn_list=(
  4
  # 32
)

ior_flags_list=(
  "-w -r"    # Single shared file
)

# ============================================================
# BenchFS-specific Parameters
# ============================================================

server_ppn_list=(
  1
  # 16
)

benchfs_chunk_size_list=(
  1m   # 1 MiB - matches small transfer
  # 4k   # 4 KiB - matches small transfer
)

# ============================================================
# CHFS-specific Parameters
# ============================================================

CHFS_CHUNK_SIZE=1048576    # 1 MiB
# CHFS_CHUNK_SIZE=4096  # 4 KiB - matches small transfer
CHFS_PROTOCOL="verbs"
CHFS_DB_SIZE="10GB"
