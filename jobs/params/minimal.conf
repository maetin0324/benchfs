#!/bin/bash
# Unified Job Parameters - Minimal (Quick Test)
# Purpose: Fast sanity check with minimal parameters
# Compatible with: BenchFS, BeeOND, CHFS
# Expected duration: ~1 minute per node

# ============================================================
# Common IOR Parameters
# ============================================================

transfer_size_list=(
  4m    # 4 MiB - verified stable
)

block_size_list=(
  64m   # 64 MiB - single test size
)

ppn_list=(
  1     # Single process per node
)

ior_flags_list=(
  "-w -r -F"  # File per process only
)

# ============================================================
# BenchFS-specific Parameters
# ============================================================

server_ppn_list=(
  1     # 1 server per node (default)
)

benchfs_chunk_size_list=(
  4m    # 4 MiB
)

# ============================================================
# CHFS-specific Parameters
# ============================================================

CHFS_CHUNK_SIZE=4194304    # 4 MiB
CHFS_PROTOCOL="sockets"
CHFS_DB_SIZE="10GB"
