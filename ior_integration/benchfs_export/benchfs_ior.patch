diff --git a/configure.ac b/configure.ac
index 344b340..3f2d720 100644
--- a/configure.ac
+++ b/configure.ac
@@ -412,6 +412,14 @@ PKG_CHECK_MODULES([FINCHFS], [finchfs],
   [with_finchfs=no])
 AM_CONDITIONAL([USE_FINCHFS_AIORI], [test x$with_finchfs != xno])
 
+# BENCHFS support
+PKG_CHECK_MODULES([BENCHFS], [benchfs],
+  [AC_DEFINE([USE_BENCHFS_AIORI], [], [Build BENCHFS backend AIORI])
+   BENCHFS_RPATH=$(pkg-config --libs-only-L benchfs | sed 's/-L/-Wl,-rpath=/g')
+   AC_SUBST(BENCHFS_RPATH)],
+  [with_benchfs=no])
+AM_CONDITIONAL([USE_BENCHFS_AIORI], [test x$with_benchfs != xno])
+
 # aws4c is needed for the S3 backend (see --with-S3, below).
 # Version 0.5.2 of aws4c is available at https://github.com/jti-lanl/aws4c.git
 # Install it something like this:
diff --git a/src/Makefile.am b/src/Makefile.am
index b60949a..a6b0b13 100755
--- a/src/Makefile.am
+++ b/src/Makefile.am
@@ -128,6 +128,13 @@ extraLDFLAGS += @FINCHFS_RPATH@
 extraLDADD += @FINCHFS_LIBS@
 endif
 
+if USE_BENCHFS_AIORI
+extraSOURCES += aiori-BENCHFS.c
+extraCPPFLAGS += @BENCHFS_CFLAGS@
+extraLDFLAGS += @BENCHFS_RPATH@
+extraLDADD += @BENCHFS_LIBS@
+endif
+
 if USE_S3_4C_AIORI
 extraSOURCES  += aiori-S3-4c.c
 if AWS4C_DIR
diff --git a/src/aiori.c b/src/aiori.c
index d24439d..5d05cd5 100644
--- a/src/aiori.c
+++ b/src/aiori.c
@@ -95,6 +95,9 @@ ior_aiori_t *available_aiori[] = {
 #endif
 #ifdef USE_LIBNFS_AIORI
 	&libnfs_aiori,
+#endif
+#ifdef USE_BENCHFS_AIORI
+	&benchfs_aiori,
 #endif
         NULL
 };
diff --git a/src/aiori.h b/src/aiori.h
index 2ce3d09..657395f 100755
--- a/src/aiori.h
+++ b/src/aiori.h
@@ -142,6 +142,7 @@ extern ior_aiori_t gfarm_aiori;
 extern ior_aiori_t chfs_aiori;
 extern ior_aiori_t finchfs_aiori;
 extern ior_aiori_t libnfs_aiori;
+extern ior_aiori_t benchfs_aiori;
 
 const ior_aiori_t *aiori_select (const char *api);
 int aiori_count (void);
